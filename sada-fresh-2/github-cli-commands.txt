#!/bin/bash
# Easiest method using GitHub CLI

cd "/Users/mo/Documents/sada-fresh2/sada-fresh-2"

# Create repository and push in one command
gh repo create iOS2 --public --source=. --remote=origin --push

# Or for private repository:
# gh repo create iOS2 --private --source=. --remote=origin --push
